# Zalo Channel

Run `bun install`, `bun run dev`, then `bun run register` to register the generated webhook URL with Zalo.

The current demo handles direct text events. Media remains disabled until official download hosts and codec fixtures are verified.
